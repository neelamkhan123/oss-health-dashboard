[pytest]
# Without this, pytest only puts tests/ itself on sys.path (no __init__.py
# there to make it a package), so `from app.main import app` 404s even
# though the container's CMD runs fine from this same working directory.
pythonpath = .
